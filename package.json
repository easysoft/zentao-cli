{
  "name": "zentao-cli",
  "version": "0.2.0",
  "type": "module",
  "description": "禅道命令行工具，支持在你喜爱的终端里访问和操作禅道数据，对 AI Agents 友好。",
  "author": "Sun Hao <sunhao@chandao.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/easysoft/zentao-cli.git"
  },
  "keywords": [
    "zentao",
    "禅道",
    "cli",
    "project-management"
  ],
  "bin": {
    "zentao": "./bin/zentao.js"
  },
  "files": [
    "bin",
    "dist",
    "skills"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run scripts/build.ts --minify",
    "build:sf": "bun run scripts/build.ts --compile --minify",
    "build:sf:all": "bun run scripts/build.ts --compile --minify --targets=all",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "coverage": "bun test --coverage",
    "prepublishOnly": "bun run typecheck && bun test && bun run build && node bin/zentao.js -V"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "commander": "^13.0.0",
    "configstore": "^7.0.0",
    "turndown": "^7.2.0",
    "zentao-api": "^0.6.8",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/turndown": "^5.0.5",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
